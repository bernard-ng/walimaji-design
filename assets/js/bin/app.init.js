$('document').ready(function () {

    $('.button-collapse').sideNav ({
        menuWidth: 300,
        edge: 'left',
    });
    $('select').material_select();

})
 $(document).ready(function () {
     $('.carousel').carousel();
 });
